<Window
    x:Class="Radius.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Radius">
    <Grid
        x:Name="RootGrid"
        Background="Transparent"
        ActualThemeChanged="RootGrid_ActualThemeChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBarDragRegion" ColumnSpacing="10" Padding="16,0,148,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image
                Width="18"
                Height="18"
                Source="Assets/AppIcon32.png"
                VerticalAlignment="Center" />
            <TextBlock
                Grid.Column="1"
                Text="Radius"
                FontSize="13"
                VerticalAlignment="Center" />
        </Grid>

        <Grid
            Grid.Row="1"
            Padding="24,16,24,24"
            RowSpacing="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Spacing="4">
                <TextBlock Text="Radius" FontSize="24" FontWeight="SemiBold" />
                <TextBlock Text="Soften the four physical screen corners with native click-through overlays." Opacity="0.72" TextWrapping="Wrap" />
            </StackPanel>

            <ToggleSwitch
                x:Name="EnabledSwitch"
                Grid.Row="1"
                Header="Corner overlays"
                Toggled="EnabledSwitch_Toggled" />

            <ToggleSwitch
                x:Name="StartupSwitch"
                Grid.Row="2"
                Header="Open on startup"
                Toggled="StartupSwitch_Toggled" />

            <Grid Grid.Row="3" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="64" />
                </Grid.ColumnDefinitions>
                <Slider
                    x:Name="RadiusSlider"
                    Minimum="0"
                    Maximum="96"
                    StepFrequency="1"
                    TickFrequency="8"
                    ValueChanged="RadiusSlider_ValueChanged" />
                <NumberBox
                    x:Name="RadiusBox"
                    Grid.Column="1"
                    Minimum="0"
                    Maximum="96"
                    SmallChange="1"
                    LargeChange="8"
                    SpinButtonPlacementMode="Compact"
                    ValueChanged="RadiusBox_ValueChanged" />
            </Grid>

            <TextBlock
                Grid.Row="4"
                Text="Tip: keep the radius close to your display's actual corner curve. Closing or hiding this window keeps Radius running in the notification area."
                Opacity="0.68"
                TextWrapping="Wrap" />

            <Border
                Grid.Row="5"
                Padding="10,8"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <TextBlock
                    x:Name="StatusText"
                    Text="Starting..."
                    Opacity="0.78"
                    TextWrapping="Wrap" />
            </Border>

            <Button
                Grid.Row="6"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Content="Hide"
                Click="HideButton_Click" />
        </Grid>
    </Grid>
</Window>
